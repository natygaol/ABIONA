<h1> What kind of trip are you looking for? </h1>


<div class="container">
  <div class="cards" id="travel-index">
  <% counter = 0 %>
    <% @travel_styles.each do |style| %>
    <% counter += 1 %>
    <%= style.title %>
    <%= image_tag style.picture %>
    <% end %>
  </div>
</div>

<%= link_to "Back", trips_path %>
